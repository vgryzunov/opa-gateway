decision_logs:
  console: true

services:
  - name: bunble-server
    url: http://localhost:8080

bundles:
  rbac:
    service: bunble-server
    resource: bundle.tar.gz
    persist: true
    polling:
      min_delay_seconds: 6
      max_delay_seconds: 30
